## \package backup Package containing modules for creating backups
#
#  A package contatining modules for effeciently creating and managing remote
#  backups using `rsync` and `ssh`.  Supports: custom backup prefix, specifying
#  rsync exclude and ssh identity files, and dry-run mode.
